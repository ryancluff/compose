#!/bin/bash
docker-compose -f "${COMPOSE}/network/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/management/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/cloud/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/media/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/download/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/minecraft/docker-compose.yml" up -d
docker-compose -f "${COMPOSE}/dev/docker-compose.yml" up -d